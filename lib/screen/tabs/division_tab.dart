import 'package:flutter/cupertino.dart';

class DivisionTab extends StatelessWidget {
  const DivisionTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
